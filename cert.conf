# Configuration for Certificate Generation
# ----------------------------------------
# Encryption cipher for private key at rest (must be supported by OpenSSL pkey, e.g., aes256)
CIPHER="aes256"

# Asymmetric Public Key Algorithm (e.g., ed25519, rsa)
ASYM_ALGO="ed25519"

# Certificate Validity Period (in days)
CA_DAYS="3650"
CERT_DAYS="365"

# Common Names (CN)
CA_CN="Default Root CA"
INTERMEDIATE_CN="Default Intermediate CA"
CLIENT_CN="Default Client Certificate"

# Output directory for certificates
OUTPUT_DIR="./certs"

# File name defaults
PRIVKEY_FILENAME="privkey.pem"
CERT_FILENAME="certs.pem"
CHAIN_FILENAME="fullchain.pem"
SECRET_FILENAME="secret.txt"
POSTINSTALL_CONF_FILENAME="postinstall.conf"

# Default deployment options (can be overridden by command line or cached in certificate directory)
# INSTALL_FULLCHAIN="/path/to/install/chain.pem"
# INSTALL_PRIVKEY="/path/to/install/privkey.pem"
# POST_SCRIPT="/path/to/script.sh"
